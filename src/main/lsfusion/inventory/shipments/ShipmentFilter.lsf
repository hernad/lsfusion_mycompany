MODULE ShipmentFilter;

REQUIRE Shipment;

NAMESPACE Inventory;

@defineDateFilterForm(shipments, s, scheduled);

// right pane
@defineDocFilter(shipment, ShipmentType, type, s, 'Type');

@defineDocFilter(shipment, Location, location, s, 'From where');

@defineDocFilter(shipment, Location, toLocation, s, 'Where');
